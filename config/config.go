package config

// DBName : Used to use a single common db name
var DBName = "peer2peer"

// APIServerPort is used to specify the port of app server
var APIServerPort = ":8000"

// AuthServerPort is used to specify the port of auth server
var AuthServerPort = ":8001"

// Cost is the cost of hashing
var Cost = 10

//AdminToken is used to control signup requests
var AdminToken = "saurabhjoshisalilpai"
